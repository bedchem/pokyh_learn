## 2026-09-19 18:48 CEST — Pokyh AI runtime verification and four-vCPU example

- Intent: Verify the reported Prisma/Docker deployment concern and the integrated Pokyh AI delivery, then align the safe example runtime settings with a four-vCPU pilot host.
- Outcome: The supplied deployment excerpt had no Prisma failure: Prisma Client generation completed. The Dockerfile installs OpenSSL in both relevant Alpine stages, and a complete Docker build passed without the prior OpenSSL detection warning. The example environment now matches Compose's four-vCPU defaults (four CPU threads, two bounded parallel requests, 10GB model headroom). Only `gemma4:e4b` is configured and present in the local Ollama runtime.
- Affected areas: `pokyh-backend/.env.example`; this worklog. Existing in-progress Pokyh AI backend, admin, and frontend changes were inspected but not overwritten.
- Verification: `npm run build`, `npx prisma validate`, `docker compose --env-file .env.example config -q`, and a full `docker compose --env-file .env.example build app` all passed in the backend. The running local API, MySQL, Redis, and Ollama containers were healthy; Ollama listed one model. In `pokyh_learn-frontend`, `npm run typecheck`, `npm run lint`, `npm run build`, and the production Docker image build passed. An isolated demo-mode UI container was also built. The available browser runner could inspect the existing public Learn page but could not connect to newly exposed local test ports, so it could not complete an interactive authenticated Pokyh AI visual pass; this is recorded as a limitation rather than treated as a successful click-through.
- Risk / next step: Four signed-in users are supported by authorization, persistent per-user conversations, server-side quotas, and bounded Ollama queuing; actual reply latency still depends on the production host's confirmed CPU/RAM and prompt lengths. Complete the browser visual pass on a host that can access the demo/test container or with a non-production test account. No code or deployment has been committed or pushed.
- Release state: uncommitted

## 2026-09-19 19:04 CEST — durable background Pokyh AI generations

- Intent: Ensure an accepted Pokyh AI request completes on the backend after its browser tab or BFF request has closed.
- Outcome: AI messages now create a durable user row and a linked queued assistant placeholder in one transaction before any model call. The backend reserves quota, atomically claims the job, persists completion or a safe retryable failure, and resumes queued/running work after an application restart. The frontend polls only while the panel is open and reloads persisted results on return; it does not own or cancel generation work.
- Affected areas: `pokyh-backend/prisma/schema.prisma`, `src/services/learnAiConversations.ts`, `src/services/learnAiRateLimit.ts`, `src/index.ts`, `src/routes/learnAi.ts`; `pokyh_learn-frontend/components/learn/ai-assistant.tsx`, `lib/i18n.ts`; this worklog.
- Verification: Prisma generation/validation and TypeScript passed; the API and frontend production builds passed. The rebuilt local API applied its additive schema successfully. A first live generation exposed a missing-counter edge case during completion accounting; it was corrected with an upserted token update. A repeat lifecycle test observed `queued` followed by a persisted non-empty `completed` reply. Finally, the real authenticated HTTP route returned `202 Accepted`; its caller exited, and a separate later database read observed the server-owned reply as `completed`.
- Risk / next step: Queued work persists through browser/BFF disconnects and is recovered on app restart. It still needs the configured local Ollama service and model to be available; a model/provider failure becomes a safe retryable status and refunds the quota reservation. No code or deployment has been committed or pushed.
- Release state: uncommitted

## 2026-09-19 19:50 CEST — unique AI sentences, synonym-aware checks, and search shortcut

- Intent: Ensure repeated AI practice does not recycle the same sentence, accept valid full-sentence synonym translations, and make the advertised search keyboard shortcut real.
- Outcome: Each AI training prompt now has a SHA-256 sentence fingerprint unique to its learner and vocabulary entry. The generator uses a new server-created variation token for every attempt and retries rather than returning a normalized duplicate. Exact server-key matching still runs first; only an exact miss gets one strict, JSON-only local-model semantic-equivalence pass. That evaluator receives the original sentence, the private reference translation, and the learner answer only as untrusted reference data; it accepts full-meaning synonyms but rejects incomplete or meaning-changing answers. If the model is unavailable, the normal deterministic result remains safe.
- UX: ⌘/Ctrl+K now navigates authenticated learners to the catalogue and focuses the functional catalogue search input. The top-bar search trigger uses the same route. Existing Escape/menu and native form keyboard behavior remain unchanged.
- Capacity/security: Sentence generation and semantic evaluation share the configured 10-slot model cap. Semantic checks count against the server-owned per-user model-work quota; answers and generated sentences remain out of audit metadata.
- Verification: Backend Prisma validation/generation and TypeScript build passed. Frontend typecheck, lint, and production build passed. Docker rebuilt successfully after one transient package-registry reset; the healthy app applied the two additive prompt-fingerprint schema statements and the live Prisma diff is empty. Browser inspection loaded the catalogue focus route with its searchable input and filters present.
- Release state: uncommitted

## 2026-09-19 19:38 CEST — replace general AI chat with vocabulary-only trainer

- Intent: Retire the general AI chat at the learner's request and retain AI only as an optional, server-authoritative vocabulary-sentence exercise.
- Outcome: The learner starts on the vocabulary word and can request one short sentence; the expected translation stays in a 15-minute server-side `LearnAiTrainingPrompt` and is revealed only for an incorrect answer. The model receives only the selected server-authorized entry and language direction, is requested to return structured JSON with thinking disabled, accepts no free-form prompt/files/page context, and cannot browse. Sentence results update the existing adaptive review state atomically. Normal cards now alternate their saved source-to-target and target-to-source directions per entry, which preserves the English↔German or Italian↔German pair of the course.
- Capacity and safety: The backend/admin setting `maxConcurrentTrainingGenerations` and Ollama's `OLLAMA_NUM_PARALLEL` both default to 10; excess model requests receive a bounded retryable response while accepted requests continue server-side if the browser disconnects. Only `gemma4:e4b` is configured and the local Ollama runtime contained exactly that model. Dictionary verification remains constrained to the configured allowlisted providers rather than arbitrary scraping.
- Retired data: The deployment cleanup removed exactly the three former general-chat tables (`learn_ai_attachments`, `learn_ai_messages`, `learn_ai_conversations`) and three chat-only configuration columns. No course, vocabulary, enrollment, review, grant, or profile records were targeted.
- UX fixes: Removed the floating chat panel and admin upload/context controls; added AI sentence creation and word skip to practice; repaired the internal Continue-learning links; and corrected the login password-visibility control's click target, labelling, and input spacing.
- Verification: Backend `npx prisma validate`, Prisma Client generation, TypeScript build, admin production build, Docker app build, container health, `/readyz`, empty live Prisma diff, configured-model inspection, and an unauthenticated trainer-route protection probe all passed. Frontend `typecheck`, lint, production build, and both repositories' `git diff --check` passed. Browser accessibility inspection confirmed the sign-in password visibility control is exposed as an interactive labelled control; no test credentials were used for an authenticated visual pass.
- Release state: uncommitted
